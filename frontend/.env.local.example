# Base URL for Netlify function API calls.
# Leave empty when deploying to Netlify (functions are on the same origin).
# Set to http://localhost:8888 when running `netlify dev` locally,
# or to your deployed site URL if testing against a live deployment.
NEXT_PUBLIC_API_BASE=
